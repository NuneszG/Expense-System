
export interface DesireProp {
    name: string
    price: string
    amount: string
};

export interface DesireUpdateProp {
    id: string
    name: string
    price: string
    amount: string
};

export interface DesireDeleteProp {
    id: string
};